use crate::interaction::Interaction;
use crate::state::UnsizedState;
use crate::system::unsized_system::UnsizedSystem;

pub fn apply(
    _system: &UnsizedSystem,
    _states: &Vec<UnsizedState>,
    _interactions: &Vec<Interaction>,
) -> UnsizedSystem {
    todo!()
}
